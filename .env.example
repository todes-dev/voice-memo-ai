# mock = demo data, no API key. gemini = Google. openai = OpenAI Whisper + GPT.
# You can also choose provider in the UI.
AI_PROVIDER=mock

# When using Gemini (transcription + summary)
# GOOGLE_GENERATIVE_AI_KEY=

# When using OpenAI (Whisper transcription + GPT summary)
# OPENAI_API_KEY=
