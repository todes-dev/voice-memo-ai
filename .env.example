# AI Provider Configuration
# Providers are auto-detected based on API keys you provide below.
# Set one or both API keys - the app will show only available providers in the UI.
# In development, "Mock" provider is always available (no API key needed).

# Google Gemini (transcription + summary)
# GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_key

# OpenAI (Whisper transcription + GPT summary)
# OPENAI_API_KEY=your_openai_key
